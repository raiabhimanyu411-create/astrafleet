# Leave empty when the backend serves client/dist from the same domain.
# For a separate backend deployment, set the full backend URL, for example:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
